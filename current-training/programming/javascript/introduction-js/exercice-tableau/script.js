// TABLEAU
let tab = [1, 2, 3, 4, 5];
console.table(tab);

// AJOUT - SUPPRESSION
let chiottes = [];

chiottes.push(10); // Ajoute la valeur 10 au tableau
chiottes.push(20);
chiottes.push(30);

chiottes.shift(); // Retire la première valeur - Shift n'a pas besoin de valeur, il ne retire que la première entrée
console.log(chiottes);
